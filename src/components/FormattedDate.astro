---
import dayjs from "dayjs";
interface Props {
  date: Date;
}

const { date } = Astro.props;
const stringDate = date.toISOString().split("T")[0];
---

<time datetime={date.toISOString()}>
  {dayjs(stringDate).format("MMM D, YYYY")}
</time>
